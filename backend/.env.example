PORT =
frontend_origin = http://localhost:4200
ai_origin = 
ai_request_timeout_ms = 15000

db_name = 
db_host = 
db_user = 
db_password = 
db_port = 
avien_ca_cert = 

# Firebase project used by the frontend. The Admin SDK uses Application Default Credentials.
FIREBASE_PROJECT_ID = natabridge-cf0da
# Set GOOGLE_APPLICATION_CREDENTIALS to the path of a service account JSON file
# when Application Default Credentials are not provided by the hosting platform.
# Alternatively, set FIREBASE_SERVICE_ACCOUNT_JSON to the complete service account JSON.
